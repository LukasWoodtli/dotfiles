
- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.zshrc: zsh/zshrc
    ~/.commonrc: shells/.commonrc
    ~/.gitconfig: git/.gitconfig
    ~/.global_gitignore: git/.global_gitignore

# Clean symlinks which have moved
- clean: ['~', '~/.config/']
